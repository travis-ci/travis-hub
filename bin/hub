#!/usr/bin/env ruby

$stdout.sync = true

$LOAD_PATH << 'lib'

require 'bundler/setup'
require 'travis/hub'

type = ARGV.shift
args = ARGV.map { |arg| arg.gsub(/\D/, '').to_i }

Travis::Hub::App.run(type, *args)
